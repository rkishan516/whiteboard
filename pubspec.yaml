name: whiteboard
description: A package for freehand use on whiteboard.
version: 0.0.1
homepage: https://github.com/rkishan516/whiteboard

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter